spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/recipe_schema?useSSL=false&serverTimezone=UTC
    username: recipe_user
    password: Drow0ssap!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update  # or `validate`, `create`, `create-drop`
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8080

#logging:
#  level:
#    org.hibernate: DEBUG
#    org.springframework: DEBUG
